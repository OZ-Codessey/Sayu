# ==============================================================================
# 1. macOS (맥) 시스템 파일
# ------------------------------------------------------------------------------
# 맥 환경에서 폴더 보기 설정, 리소스 포크, 네트워크/구버전 작업 시 생성되는
# 숨김 시스템 파일이 깃허브에 올라가는 것을 차단합니다.
# ==============================================================================
.DS_Store
.AppleDouble
.LSOverride
._*

# ==============================================================================
# 2. Windows (윈도우) 시스템 파일
# ------------------------------------------------------------------------------
# 윈도우 환경에서 썸네일 미리보기, 폴더 스타일 설정, 휴지통 관련
# 숨김 시스템 파일이 깃허브에 올라가는 것을 차단합니다.
# ==============================================================================
Thumbs.db
desktop.ini
$RECYCLE.BIN/
*.stackdump

# ==============================================================================
# 3. Linux / Unix (리눅스 / 유닉스) 시스템 파일
# ------------------------------------------------------------------------------
# 리눅스 데스크톱 환경이나 터미널 작업 중 자동 생성되는 임시 백업 파일 등을 차단합니다.
# ==============================================================================
*~
.directory
.fuse_hidden*

# ==============================================================================
# 4. 개발 에디터 및 IDE 설정 파일
# ------------------------------------------------------------------------------
# 개발자 개인의 에디터 설정(VS Code, IntelliJ 등)이 저장소에 올라가
# 다른 사용자의 개발 환경과 충돌을 일으키는 것을 차단합니다.
# ==============================================================================
.vscode/
.idea/
*.swp
*.swo

# ==============================================================================
# 5. 보안 및 환경변수 설정 파일
# ------------------------------------------------------------------------------
# API 키, 비밀번호, 개인 인증 정보 등이 포함된 환경변수 파일의 실수 유출을 막습니다.
# ==============================================================================
.env
.env.local
*.pem

# ==============================================================================
# 6. 주요 언어 및 프레임워크 자동 생성/라이브러리 폴더 (필수 제외)
# ------------------------------------------------------------------------------
# 용량이 매우 크거나, 각 컴퓨터 환경에서 자동 생성되어 깃허브에 올리면 안 되는
# 패키지 및 가상환경, 캐시 폴더를 차단합니다.
# ==============================================================================
node_modules/
__pycache__/
*.pyc
.venv/
venv/
build/
dist/